import { createContext } from "react";

// export const counterContext = createContext(initial_VALUE)
export const counterContext = createContext(0)

// Now we import this in our main folder ContextApi.jsx